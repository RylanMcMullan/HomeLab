# Cloudflare Tunnel

**Status:** planned; not deployed for the HomeLab website.

The intended use is securely exposing the planned portfolio website. Tunnel name/ID, Cloudflare account and zone details, domain, DNS records, connector host, access policy, and configuration are `UNKNOWN` until deployment is verified.

Cloudflare tokens, tunnel credentials, JSON credentials, API tokens, and origin authentication material are secrets and must never be committed. Track work in [ROADMAP.md](../../ROADMAP.md#planned).
